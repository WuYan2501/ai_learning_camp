# ⑨ 实操技能：AI 工程师日常实战技能库

> 从理论到落地的关键一跃，覆盖数据处理、Prompt 调优、模型选型、调试排错、Harness 评测与项目实战全流程
>
> 返回 [知识库索引](../SYLLABUS.md)

---

## 9.1 数据处理与准备

### 数据清洗 🔥

- **文本清洗流水线**
  - 去重（MinHash / SimHash）→ 去噪（HTML标签/特殊字符/乱码）→ 长度过滤 → 语言检测 → 质量打分
  - *核心思想*：数据质量 > 数据数量，垃圾数据进 = 垃圾模型出（Garbage In, Garbage Out）
  - *为什么重要*：DeepSeek/LLaMA 的训练数据处理比模型架构更关键
  - *核心问题*：如何判断数据质量？（困惑度过滤 / 分类器打分 / 规则过滤组合）

- **数据去重策略** ⭐
  - 精确去重：Hash 匹配（MD5/SHA256），速度快但只能处理完全一致的文本
  - 模糊去重：MinHash + LSH（局部敏感哈希），处理近似重复
  - 语义去重：Embedding 相似度 > 阈值的文本合并/删除
  - *核心问题*：去重阈值怎么设？（太严格丢信息，太宽松留冗余，通常 0.8-0.9）

### SFT 数据构造 🔥🔥

- **高质量指令数据的核心要素**
  - 多样性（任务类型覆盖广）> 数量（1K 高质量 > 100K 低质量）
  - 复杂度阶梯（简单→中等→复杂，覆盖不同难度）
  - 格式一致性（统一的 instruction/input/output 格式）
  - *核心问题*：SFT 数据量多少合适？（1K-10K 高质量通常够用，关键是多样性和质量）

- **合成数据技巧** 🔥 ⭐
  - Self-Instruct：用 LLM 生成指令数据，再用 LLM 过滤低质量数据
  - Evol-Instruct：从简单指令逐步进化出复杂指令（WizardLM 方案）
  - 种子数据扩充：人工写 100 条高质量种子 → LLM 扩写到 10K 条
  - *核心问题*：合成数据的天花板在哪？（数据多样性受限于生成模型的能力边界）
  - *延伸*：用强模型（GPT-4/Claude）生成 → 弱模型学习，是当前性价比最高的方案

- **RLHF/DPO 偏好数据**
  - Chosen/Rejected 对的构造：同一 Prompt 让不同模型生成 → 人工/LLM 排序
  - 偏好数据质量控制：标注一致性（Cohen's Kappa > 0.7）/ 覆盖多种错误类型
  - *核心问题*：偏好数据的 Chosen 和 Rejected 差距多大合适？（差距太小学不到，太大学过头）

### 数据格式与工具

- **常用格式**
  - Alpaca 格式：`{"instruction": "", "input": "", "output": ""}`
  - ShareGPT 格式：`{"conversations": [{"from": "human", "value": ""}, {"from": "gpt", "value": ""}]}`
  - *核心问题*：多轮对话怎么组织？（ShareGPT 格式更自然，支持多轮上下文）

- **数据标注工具**
  - Label Studio：开源，支持文本/图像/音频多模态标注
  - Argilla：专注 NLP，和 HuggingFace 生态集成好
  - Prodigy：商业工具，主动学习加速标注

> 💡 **面试拷问 — 数据处理**
> - 你的 SFT 数据是怎么构造的？质量怎么保证？⭐
> - 合成数据和真实数据的比例怎么控制？⭐
> - 数据去重用什么方案？MinHash 的原理是什么？⭐
> - 偏好数据标注中遇到过什么问题？标注一致性怎么保证？
> - 如何用少量种子数据扩充出高质量训练集？

---

## 9.2 Prompt 工程实战技能 🔥🔥

### Prompt 调试方法论

- **迭代调优四步法**
  1. 明确目标：定义输入/输出格式和评估标准
  2. 基线 Prompt：先写最简单直接的版本
  3. 错误分析：收集失败 case，分类错误类型
  4. 针对性优化：对每类错误添加约束/示例/格式要求
  - *核心思想*：Prompt 工程是实验科学，不是一次写对，而是迭代优化
  - *核心问题*：如何系统化管理 Prompt 迭代？（版本控制 + A/B 测试 + 指标追踪）

- **常见失败模式与修复** ⭐
  - 输出格式不稳定 → 明确要求 JSON 输出 + 示例 + schema 约束
  - 忽略部分指令 → 指令编号 + 强调关键约束 + 检查清单
  - 幻觉严重 → 引入 RAG + 要求引用来源 + "如果不确定请说不知道"
  - 推理错误 → 添加 CoT 提示 + 分步骤拆解 + 自我验证
  - 上下文窗口溢出 → 摘要压缩 + 关键信息前置 + 分段处理

### System Prompt 设计模式 ⭐

- **角色 + 任务 + 约束 + 格式**（RTCF 模式）
  - Role：你是一个...（定义身份和专业领域）
  - Task：你的任务是...（明确目标）
  - Constraints：注意以下约束...（边界条件和负面示例）
  - Format：请按以下格式输出...（结构化输出）
  - *核心思想*：好的 System Prompt 像一份岗位说明书

- **少样本提示的技巧**
  - 示例选择：覆盖边界 case + 典型 case，不只选简单的
  - 示例顺序：难度递进，最后一个示例最接近实际场景
  - 示例数量：3-5 个通常最佳，太多可能引入噪音
  - *核心问题*：Zero-shot vs Few-shot 怎么选？（先试 Zero-shot，不行再加 Few-shot）

### 结构化输出控制 🔥

- **JSON 模式**
  - OpenAI response_format=json_object / json_schema
  - 手动约束：提示词中给出完整 JSON schema + 示例
  - *核心问题*：为什么 JSON 输出有时会格式错误？（模型生成是概率性的，需要约束解码）

- **约束解码（Constrained Decoding）**
  - Outlines / Guidance / SGLang：在 token 级别强制遵循 schema
  - *核心思想*：在采样时 mask 掉不符合语法的 token，保证 100% 格式正确
  - *核心问题*：约束解码会影响生成质量吗？（轻微影响，但格式保证值得这个代价）

> 💡 **面试拷问 — Prompt 实战**
> - 你遇到过最难调的 Prompt 是什么场景？怎么解决的？⭐
> - Prompt 版本管理你们怎么做？怎么做 A/B 测试？⭐
> - 如何减少 LLM 输出的幻觉？除了 RAG 还有什么方案？⭐
> - 结构化输出怎么保证格式 100% 正确？⭐
> - System Prompt 太长会影响性能吗？怎么优化？

---

## 9.3 模型选型与评估决策 🔥🔥

### 模型选型决策树

- **按场景选模型** ⭐
  ```
  需求分析
  ├── 简单分类/提取 → 小模型（7B-14B）或 API
  ├── 复杂推理/代码 → 大模型（70B+）或推理模型（o3/R1）
  ├── 多模态理解 → GPT-4o / Gemini 2.5 / Claude 3.7
  ├── 长上下文（>32K）→ Gemini（1M）/ Claude（200K）/ GPT-4o（128K）
  ├── 私有化部署 → 开源模型（Qwen/LLaMA/DeepSeek）
  └── 低成本高吞吐 → 小模型 + 量化 / API 的 mini 版本
  ```

- **开源 vs 闭源决策** ⭐
  - 闭源 API 优先：快速原型 / 数据不敏感 / 追求最强效果
  - 开源自部署：数据安全 / 成本敏感 / 需要定制微调
  - 混合方案：用 API 做 Teacher 生成数据 → 训练开源 Student 模型
  - *核心问题*：什么时候应该从 API 切换到自部署？（日调用量 > 10K + 延迟要求严格 + 数据合规）

- **模型能力快速评估方法**
  - 先用 3-5 个代表性 case 手动测试（覆盖简单/复杂/边界场景）
  - 再用 50-100 个标注数据做自动化评估
  - 最后看 Chatbot Arena / Open LLM Leaderboard 的排名做参考
  - *核心问题*：公开 Benchmark 和实际业务效果差距大怎么办？（必须用自己的评估集）

### 成本优化实战 🔥

- **Token 用量优化**
  - Prompt 瘦身：删除冗余指令 / 压缩示例 / 使用缩写
  - 输出控制：限制 max_tokens / 要求简洁输出
  - 缓存策略：相同 Prompt 前缀缓存（Anthropic Prompt Caching / SGLang RadixAttention）
  - *核心问题*：Prompt Caching 能省多少钱？（Claude 的 Prompt Caching 可省 90% 输入 Token 费用）

- **API 调用策略**
  - 路由分级：简单任务用便宜模型 → 失败或不确信时 fallback 到强模型
  - Batch API：非实时任务用 Batch 模式，通常 50% 折扣
  - *核心问题*：模型路由怎么做？（训练一个小分类器判断任务难度，或用规则 + 置信度分流）

> 💡 **面试拷问 — 模型选型**
> - 你们项目的模型是怎么选的？为什么选这个？⭐
> - 开源和闭源模型怎么选？考虑哪些因素？⭐
> - 如何在效果和成本之间找到平衡？⭐
> - API 调用成本怎么优化？有具体数据吗？
> - 评估一个模型你会看哪些指标？公开 Benchmark 你信吗？⭐

---

## 9.4 RAG 实战技能 🔥🔥

### 文档处理流水线

- **解析阶段**
  - PDF 解析：PyMuPDF（快速）/ Unstructured（通用）/ Marker（学术论文）
  - 表格提取：Camelot / Tabula / LLM 辅助提取
  - 图片/OCR：PaddleOCR / Tesseract / 多模态 LLM 直接理解
  - *核心问题*：PDF 解析质量差怎么办？（多工具组合 + LLM 后处理修复）

- **分块策略** ⭐
  - 固定长度分块：简单，但可能截断语义
  - 语义分块：按段落/章节/主题切分，保持语义完整
  - 递归分块：先大块再递归细分，LangChain 默认方案
  - 滑动窗口：chunk 之间有重叠（overlap），防止边界信息丢失
  - *核心问题*：chunk_size 和 overlap 怎么设？（通常 512-1024 tokens，overlap 10-20%）
  - *核心问题*：chunk_size 太大和太小分别有什么问题？（太大→噪音多检索不准，太小→上下文不完整）

### 检索优化 ⭐

- **Embedding 选型**
  - 通用：OpenAI text-embedding-3-large / Cohere embed-v3
  - 开源：BGE-M3 / GTE / E5-Mistral（MTEB 排名参考）
  - *核心问题*：Embedding 维度越高越好吗？（不一定，要考虑存储成本和检索速度）

- **混合检索（Hybrid Search）** 🔥
  - 向量检索（语义）+ BM25/关键词检索（精确）→ RRF（Reciprocal Rank Fusion）融合
  - *为什么重要*：纯向量检索在精确匹配（人名/编号/专业术语）上表现差
  - *核心问题*：向量检索和关键词检索的权重怎么设？（通常 0.7:0.3，需根据场景调整）

- **Reranker** 🔥 ⭐
  - Cross-Encoder 重排序：Cohere Rerank / BGE-Reranker / Jina Reranker
  - *核心思想*：初检索 top-50 → Reranker 精排 → 取 top-5 给 LLM
  - *为什么比 Embedding 更准*：Cross-Encoder 可以同时看 query 和 document，交互更深
  - *核心问题*：Reranker 会增加多少延迟？（通常 50-100ms，值得这个代价）

### RAG 调试排错

- **常见问题诊断**
  - 检索不到相关内容 → 检查分块策略 / Embedding 质量 / 查询改写
  - 检索到了但答案错误 → LLM 没有正确利用上下文 / 上下文太多有噪音
  - 幻觉严重 → 强化"仅基于给定内容回答" + 引用来源
  - 响应慢 → 检索阶段优化（索引/缓存）+ LLM 流式输出
  - *核心思想*：RAG 调试要分层诊断——检索层 vs 生成层

- **评估指标** ⭐
  - 检索质量：Recall@K / MRR / NDCG
  - 生成质量：Faithfulness（忠实度）/ Answer Relevancy / Context Precision
  - 端到端：用 RAGAS 框架自动化评估
  - *核心问题*：RAGAS 的四个核心指标分别是什么？（Faithfulness / Answer Relevancy / Context Precision / Context Recall）

> 💡 **面试拷问 — RAG 实战**
> - 你的 RAG 系统 chunk_size 设的多少？为什么？⭐
> - 混合检索怎么做？向量和关键词的权重怎么调？⭐
> - RAG 系统的检索准确率怎么评估？⭐
> - 遇到过什么 RAG 的坑？怎么解决的？⭐
> - Reranker 用了吗？效果提升多少？⭐

---

## 9.5 Agent 开发实战技能 🔥🔥🔥

### 工具定义与 Function Calling

- **工具设计原则** ⭐
  - 单一职责：每个工具只做一件事，名称和描述要清晰明确
  - 参数设计：类型明确 + 默认值合理 + 必选/可选区分清楚
  - 错误处理：返回结构化错误信息，让 Agent 能理解并重试
  - *核心思想*：工具的 description 就是给 LLM 看的文档，写不好 LLM 就调不对
  - *核心问题*：工具数量多了 LLM 调用准确率下降怎么办？（工具分组 + 路由 + 动态加载）

- **Function Calling 调试** 🔥
  - 常见错误：参数类型错误 / 遗漏必选参数 / 选错工具 / 不调用工具
  - 调试方法：打印完整的 messages 和 tool_calls → 分析 LLM 的决策过程
  - 优化方法：优化工具描述 → 添加示例 → 减少工具数量 → 换更强的模型
  - *核心问题*：并行工具调用怎么实现？（OpenAI 原生支持 parallel_tool_calls）

### Agent 架构选型

- **单 Agent vs 多 Agent** ⭐
  - 单 Agent：简单任务，工具 < 10 个，流程线性
  - 多 Agent：复杂任务，需要不同专业角色协作，有并行子任务
  - *核心问题*：什么时候该从单 Agent 升级到多 Agent？（单 Agent 工具调用准确率 < 80% / 任务复杂度需要专业分工）

- **编排模式选型**
  - 线性链（Sequential）：A → B → C，简单管道
  - 路由器（Router）：根据输入分发到不同 Agent
  - 状态机（State Machine）：LangGraph 风格，节点 + 条件边
  - 层级式（Hierarchical）：Manager Agent 分配任务给 Worker Agent
  - *核心问题*：LangGraph 和 CrewAI 怎么选？（需要精细控制用 LangGraph，快速原型用 CrewAI）

### Agent 可靠性工程 🔥

- **错误恢复策略** ⭐
  - 重试机制：工具调用失败 → 指数退避重试（最多 3 次）
  - 降级策略：强模型失败 → fallback 到弱模型 / 直接返回部分结果
  - 人工介入：关键决策设置断点，需要人工确认后继续
  - *核心问题*：Agent 进入死循环怎么办？（设置最大步数 + 超时 + 循环检测）

- **上下文管理** ⭐
  - 对话历史压缩：超过窗口时自动摘要 / 只保留最近 N 轮 + 系统提示
  - 工具结果处理：大结果截断/摘要，避免上下文溢出
  - 记忆系统：短期记忆（对话上下文）+ 长期记忆（向量数据库持久化）
  - *核心问题*：Agent 的上下文窗口满了怎么处理？（摘要压缩 vs 滑动窗口 vs 分段执行）

- **安全防护**
  - 输入验证：过滤注入攻击 / 限制输入长度
  - 工具权限：敏感操作需要二次确认 / 白名单机制
  - 输出过滤：检查生成内容是否包含敏感信息
  - *核心问题*：Prompt Injection 在 Agent 中为什么更危险？（Agent 有工具调用能力，注入后可以执行危险操作）

> 💡 **面试拷问 — Agent 实战**
> - 你的 Agent 系统架构是什么样的？为什么这么设计？⭐
> - Agent 的工具调用准确率怎么保证？⭐
> - 遇到过 Agent 进入死循环吗？怎么解决的？⭐
> - Agent 的上下文管理怎么做？窗口满了怎么办？⭐
> - Agent 的安全防护做了哪些？⭐

---

## 9.6 微调实战技能 🔥

### 微调决策

- **该不该微调？** ⭐
  ```
  评估路径：
  Prompt 优化能解决吗？ ── 能 → 不微调
       │
       否
       ▼
  RAG 能解决吗？ ── 能 → 不微调
       │
       否
       ▼
  有足够高质量数据（>500条）？── 没有 → 先构造数据
       │
       有
       ▼
  开始微调
  ```
  - *核心思想*：微调是最后手段，Prompt + RAG 能解决就不微调
  - *核心问题*：什么场景必须微调？（特定输出格式/风格 / 领域知识内化 / 延迟敏感用小模型）

### LoRA 微调实战 🔥 ⭐

- **超参数选择**
  - rank（r）：通常 8-64，任务越复杂越大，r=16 是常见起点
  - alpha：通常 = 2×rank 或 = rank
  - target_modules：通常选 q_proj, v_proj，全量选 all-linear 效果更好
  - learning_rate：通常 1e-4 ~ 2e-5，比全量微调大一个量级
  - *核心问题*：rank 设多大合适？怎么判断 rank 不够？（验证集 loss 不再下降 + 任务准确率达标）

- **常见踩坑** ⭐
  - 过拟合：训练集 loss 很低但泛化差 → 减少 epoch / 加 dropout / 增加数据多样性
  - 灾难性遗忘：微调后模型通用能力下降 → 混入通用指令数据（10-20%）
  - 格式不学习：模型不遵循输出格式 → 数据中格式要一致 / 增加格式示例
  - *核心问题*：怎么判断微调后模型变好了还是变差了？（对比基线模型 + 多维度评估）

### 微调工具实操

- **Unsloth 实操要点**
  - 安装：`pip install unsloth`，自动检测 GPU 型号并优化
  - 关键参数：`load_in_4bit=True`（QLoRA）/ `max_seq_length` / `dtype`
  - 导出格式：GGUF（本地推理）/ 合并后 safetensors（vLLM 部署）
  - *优势*：训练速度比 HuggingFace 快 2x，显存省 60%

- **LLaMA-Factory 实操要点**
  - WebUI 模式：适合快速实验，可视化调参
  - CLI 模式：`llamafactory-cli train config.yaml`，适合批量实验
  - 数据格式：支持 Alpaca / ShareGPT / 自定义格式
  - *优势*：支持 100+ 模型，一站式覆盖 SFT/RLHF/DPO

> 💡 **面试拷问 — 微调实战**
> - 你微调过什么模型？数据怎么准备的？⭐
> - LoRA 的 rank 和 alpha 怎么选？为什么？⭐
> - 微调后灾难性遗忘怎么解决？⭐
> - 微调 vs Prompt Engineering vs RAG，你的决策流程是什么？⭐
> - QLoRA 和全量 LoRA 的效果差距有多大？什么时候值得用全量？

---

## 9.7 调试与排错方法论

### LLM 应用调试框架

- **分层调试法** ⭐
  ```
  问题出现
  ├── 第1层：输入层 → 检查 Prompt 是否正确、上下文是否完整
  ├── 第2层：模型层 → 换更强模型试试、调 Temperature/Top-p
  ├── 第3层：输出层 → 解析逻辑是否正确、格式是否符合预期
  ├── 第4层：集成层 → API 调用参数、超时设置、并发控制
  └── 第5层：数据层 → 训练/检索数据质量、数据分布是否偏移
  ```
  - *核心思想*：从最容易检查的层开始，逐层排查

- **Prompt 调试技巧**
  - 打印完整的 messages 数组（包括 system/user/assistant/tool）
  - 用 LangFuse/LangSmith 追踪完整调用链
  - 对比不同 Prompt 版本的输出差异
  - *核心问题*：线上 Prompt 效果突然变差怎么排查？（模型版本更新 / 输入数据分布变化 / 上下文溢出）

### 性能优化排查

- **延迟优化** ⭐
  - TTFT 高 → 检查 Prompt 长度 / 是否有 Prompt Caching / Prefill 阶段优化
  - TPS 低 → 检查 Batch 设置 / 模型量化 / 硬件利用率
  - 端到端慢 → 检查检索耗时 / 网络延迟 / 串行调用是否可并行

- **成本排查**
  - Token 统计：用 tiktoken 精确计算每次调用的 input/output tokens
  - 热点分析：哪些接口/场景消耗 token 最多
  - 优化方向：Prompt 瘦身 → 缓存 → 模型降级 → Batch API

### 可观测性建设 🔥

- **核心监控指标**
  - 延迟：P50/P95/P99 延迟、TTFT、TPS
  - 质量：LLM-as-Judge 自动打分、用户反馈（👍👎）
  - 成本：每次调用的 token 费用、日/月成本趋势
  - 可靠性：API 成功率、超时率、重试率

- **推荐工具链**
  - LangFuse（开源，调用链追踪 + 评估 + 成本统计）
  - LangSmith（LangChain 官方，和 LangChain 集成最好）
  - Helicone（API 代理模式，接入最简单）

> 💡 **面试拷问 — 调试排错**
> - LLM 应用上线后效果变差，你怎么排查？⭐
> - 你们的 LLM 应用监控了哪些指标？⭐
> - 调用延迟太高怎么优化？你做过哪些优化？⭐
> - 如何做到 LLM 应用的可观测性？⭐

---

## 9.8 Harness / 回放 / 回归评测实战 🔥🔥🔥

### 为什么现在必须会 Harness ⭐

- *核心思想*：LLM / Agent 项目不是"写完就结束"，而是每次改 Prompt、Model、Tool、Workflow 都要重新验证
- *为什么它是热门技能*：很多团队真正缺的不是再写一个 Demo，而是把 Demo 变成"可迭代、可对比、可回归"的系统
- *核心问题*：什么时候必须搭 Harness？（准备上线 / 需要多人协作 / Agent 有多步推理和工具调用 / 线上经常回归）

### 最小可用 Harness 搭建法 ⭐

1. 收集 20-50 个高价值 case（高频 + 边界 + 失败）
2. 给每个 case 定义 pass / fail 标准
3. 固定模型版本、Prompt 版本、工具版本
4. 每次改动自动跑全量 case
5. 输出 diff 报告：成功率 / 失败类型 / 成本 / 延迟变化

- *核心问题*：为什么先做小 Harness，而不是一上来做超大基准？（先把高价值 case 跑通，最容易产生业务价值）

### Case 设计技巧

- **高频 case**：覆盖 80% 的真实请求
- **失败 case**：把历史翻车样本沉淀成回归测试
- **边界 case**：超长输入 / 歧义问题 / 工具异常 / 空结果
- **对抗 case**：Prompt Injection / 恶意输入 / 权限绕过尝试
- *核心问题*：Case 集为什么会越跑越值钱？（它沉淀的是团队真实踩坑历史）

### 不同场景的 Harness 技能重点

- **RAG**
  - 检索 Recall@K、引用正确率、Faithfulness、答案完整性
- **Agent**
  - 工具选择正确率、参数填写正确率、步数、重试次数、死循环率
- **Coding**
  - 单元测试通过率、patch 成功率、环境复现能力、sandbox 安全
- **工作流 / 编排系统**
  - 节点成功率、fallback 命中率、端到端成功率、关键节点耗时

### 常见评测方式组合 ⭐

- **规则评测**：格式、关键词、schema、单元测试
- **LLM-as-Judge**：回答质量、解释完整性、偏好排序
- **人工抽检**：关键任务、争议 case、Judge 校准
- *核心问题*：为什么不能只用单一指标？（LLM 应用常常"格式对了但答案错了"，或者"答案像对的但不可执行"）

### 常见踩坑

- 只有 demo，没有回归集
- 只看最终答案，不看 tool trace / trajectory
- LLM Judge 没有校准，结果漂移严重
- 每次改 Prompt 都靠手工点点点验证
- 线上失败 case 没有回流到测试集

> 💡 **面试拷问 — Harness 实战技能**
> - 你们如何验证一次 Prompt / Model 改动到底有没有变好？⭐
> - 你的回归集是怎么构建的？失败 case 如何回流？⭐
> - RAG、Agent、Coding 三类系统的 Harness 指标分别该怎么看？⭐
> - 为什么 LLM 应用需要 replay / trace，而不是只看最终输出？⭐

---

## 9.9 项目实战方法论

### AI 项目全流程

- **需求 → POC → MVP → 生产**
  ```
  Phase 1: 需求分析（1-2天）
  ├── 明确业务目标和成功指标
  ├── 评估 AI 可行性（能做到什么程度）
  └── 确定技术方案（API/RAG/Agent/微调）

  Phase 2: POC 验证（3-5天）
  ├── 用最简单的方案验证可行性
  ├── 收集 50-100 个测试 case
  └── 初步评估效果（准确率/延迟/成本）

  Phase 3: MVP 开发（1-2周）
  ├── 核心功能开发 + 基础错误处理
  ├── 接入监控和日志
  └── 内部测试 + 迭代优化

  Phase 4: 生产上线（持续）
  ├── 灰度发布 + A/B 测试
  ├── 建立评估体系 + 持续监控
  └── 数据飞轮（用户反馈 → 数据 → 模型优化）
  ```
  - *核心思想*：快速验证，迭代优化，不要一开始就追求完美

### 效果评估体系 ⭐

- **评估维度**
  - 功能性：任务完成率 / 准确率 / 格式正确率
  - 体验性：响应延迟 / 流畅度 / 用户满意度
  - 经济性：单次调用成本 / 日均成本 / ROI
  - 安全性：幻觉率 / 有害内容率 / 数据泄露风险

- **评估方法组合**
  - 自动化评估：LLM-as-Judge + RAGAS + 规则检查
  - 人工评估：抽样人工打分（关键场景 + 边界 case）
  - 在线评估：A/B 测试 + 用户反馈 + 业务指标（转化率/满意度）
  - *核心问题*：自动化评估和人工评估差异大怎么办？（校准评估 Prompt + 增加评估维度）

### 数据飞轮 🔥

- **用户反馈 → 数据 → 模型优化 → 更好的用户体验 → 更多反馈**
  - 收集用户的 👍👎 反馈 → 构造偏好数据
  - 收集失败 case → 修复后加入训练集
  - 定期重新评估 → 发现新的问题类型 → 针对性优化
  - *核心思想*：AI 应用是活的，需要持续迭代，不是部署了就结束
  - *核心问题*：冷启动阶段没有用户数据怎么办？（合成数据 + 内部标注 + 专家反馈）

> 💡 **面试拷问 — 项目方法论**
> - 描述一个你做过的 AI 项目全流程？⭐
> - 你的项目效果怎么评估？有具体数据吗？⭐
> - 上线后效果不达预期怎么迭代？⭐
> - 数据飞轮是什么？你们项目有实现吗？⭐
> - 如果让你从零搭建一个 AI 应用，你会怎么规划？⭐